{
  "name": "ringcrypt-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "RingCrypt landing page: cross-vendor fully homomorphic encryption on GPU. Live in-browser NTT self-test and threshold secret-sharing 'private mean' demo.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "svelte": "^5.0.0",
    "vite": "^5.0.0"
  }
}
